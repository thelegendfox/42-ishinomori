import database from "better-sqlite3";
import dotenv from "dotenv";
dotenv.config();

/**
 * The standard SQL operator.
 */
// const sql = postgres(process.env.POSTGRES_URL!, { ssl: "require" });
const sql: unknown = undefined;

export default sql;
